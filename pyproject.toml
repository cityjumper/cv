[project]
name = "gareth-thomas-cv"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "zensical>=0.0.51",
]

[project.optional-dependencies]
pdf = [
    "markdown>=3.10.2",
    "pymdown-extensions>=11.0.1",
    "weasyprint>=69.0",
]
